= form_for @user_theme do |f|
  - if @user_theme.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user_theme.errors.count, "error")} prohibited this user_theme from being saved:"
      %ul
        - @user_theme.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :theme
    = f.text_field :theme
  .actions
    = f.submit 'Save'
