%h1 Listing towns #{'of '+@country.country_name if params[:country_id] != nil?}
= form_tag '#', :method => 'get', :remote => true do
  %p
    = text_field_tag :search, params[:search], autofocus: true 
    = submit_tag "Search", :name => nil
  #towns= render "towns/towns"
= link_to 'New Town', new_town_path, class: 'btn'
